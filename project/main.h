#ifndef SSP_MAIN
#define SSP_MAIN

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "utils.h"
#include "ssp_sequential.h"

#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

#endif